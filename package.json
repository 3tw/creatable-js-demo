{
  "scripts": {
    "development": "mix",
    "watch": "mix watch",
    "watch-poll": "mix watch -- --watch-options-poll=1000",
    "hot": "mix watch --hot",
    "production": "mix --production",
    "gh-pages": "git checkout gh-pages && git merge main && git subtree push --prefix creatable-js-demo origin gh-pages && git checkout main"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/3tw/creatable-js-demo.git"
  },
  "devDependencies": {
    "cross-env": "^7.0",
    "laravel-mix": "^6.0.19",
    "lodash": "^4.17.20",
    "resolve-url-loader": "^3.1.3",
    "sass": "^1.20.1",
    "sass-loader": "^8.0.0"
  },
  "dependencies": {
    "creatable-js": "^1.0.0-alpha"
  }
}
